name: Build and Deploy

on:
  push:
    branches: [ main ]

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install
        working-directory: anime/my-flowchart-app  # 👈 this is the key

      - name: Build Project
        run: npm run build
        working-directory: anime/my-flowchart-app  # 👈 run inside same folder

      - name: Upload Build Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: anime/my-flowchart-app/dist  # adjust if your build output is elsewhere

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
